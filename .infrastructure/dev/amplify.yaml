type: Amplify
spec:
  app_name: "platform-maturity-model"
  strategy:
    static:
      branch_name: "master"
  platform: 'WEB'
  build_spec: |
    version: 1
    frontend:
      artifacts:
        baseDirectory: /
        files:
          - index.html